import { BlizzConfig } from '@blizz/ui';

export const blizzConfig: BlizzConfig = {
  base: 'blizz',
  theme: 'crystal',
  components: {
    chip: {
      elements: {
        base: {
          styles: {
            padding: '8rem',
          },
        },
      },
    },
  },
};
